# Brent Oil Flask API

The Flask backend exposes the Brent oil analysis results to the React
dashboard.

## Run the API

From the project root:

```powershell
venv\Scripts\Activate.ps1
python -m backend.app